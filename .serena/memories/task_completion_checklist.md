# Task Completion Checklist

1. `npm run typecheck` — ensure no type errors
2. `npm run lint` — ensure no lint errors
3. `npm test` — ensure all 714 tests pass
4. `npm run format:check` — ensure formatting is correct
